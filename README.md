# Webpack template

## Overview

My webpack template with:
- Debug dev server
- Babel es15 preset

todo:
- tests
- async/await

## Usage

- `npm install` for first start
- `npm start` for debug
- `npm run build` for build
